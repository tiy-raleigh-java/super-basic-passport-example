<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Passport Example</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <header>
      {{#if username}}
        <!-- if the user is logged in, show their name -->
        <p class="welcome">Welcome, {{username}}!</p>
      {{/if}}

      <nav>
        <ul>
          <li><a href="/">Visit a public page.</a></li>
          <li><a href="/private">Visit a private page.</a></li>
          <li>
            <!-- if the user is logged in show a log out button or, if not, show a login button -->
            {{#if username}}
              <a href="/logout">Log out</a>
            {{else}}
              <a href="/login">Log in</a>
            {{/if}}
          </li>
        </ul>
      </nav>
    </header>

    <article>
      {{{body}}}
    </article>
  </body>
</html>
